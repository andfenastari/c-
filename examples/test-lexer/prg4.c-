int tirazeros (int n, int v[])
{
   int i, j;
   i = 0;
   for (j = 0; j &< n; j = j + 1)   /* operador invalido */
      if (v[j] != 0)
         {
          v[i] = v[j];
          i++;
         }
   return i;
}
