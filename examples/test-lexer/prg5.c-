
/* Comentario que nao fecha */
int tira0 (int n, int v[]) {
   int i = 0; int z = 0;
   for (i = 0; i < n - z; ++i) {
      if (v[i] == 0) {
         int y = z;
         z = z + 1;
         for (k = i; k < n - z; k++) v[k] = v[k+1];
         i--;
      }
   }
   return n - z;
}
